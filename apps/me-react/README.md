# TanStack Router App (Vite SPA)

This app now runs as a standard Vite single-page application using TanStack Router file-based routing.

## Getting Started

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Quality Checks

```bash
npm run test
npm run lint
npm run format
npm run check
```

## Project Structure

- `src/main.tsx`: SPA entry point (`RouterProvider` mount)
- `src/router.tsx`: Router creation and defaults
- `src/routes/*`: File-based routes
- `src/routeTree.gen.ts`: Auto-generated by the TanStack Router Vite plugin

## Routing

To add a route, create a new file in `src/routes`.

Example:

```tsx
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/profile')({
  component: ProfilePage,
})

function ProfilePage() {
  return <div>Profile</div>
}
```

Use `Link` for SPA navigation:

```tsx
import { Link } from '@tanstack/react-router'

<Link to="/about">About</Link>
```

## Styling

This project uses Tailwind CSS with custom tokens in `src/styles.css`.

## Docs

- TanStack Router: https://tanstack.com/router/latest/docs/framework/react/overview
- Vite: https://vite.dev/
