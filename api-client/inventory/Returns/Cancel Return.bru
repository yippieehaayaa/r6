meta {
  name: Cancel Return
  type: http
  seq: 6
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/returns/{{returnRequestId}}/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
