meta {
  name: Get Stock Count
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/tenants/{{tenantSlug}}/stock-counts/{{stockCountId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
