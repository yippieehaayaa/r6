meta {
  name: Check Availability
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/tenants/{{tenantSlug}}/availability
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  variantId: {{variantId}}
  warehouseId: {{warehouseId}}
}
