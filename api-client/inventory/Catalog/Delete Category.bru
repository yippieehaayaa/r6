meta {
  name: Delete Category
  type: http
  seq: 12
}

delete {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/categories/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
