meta {
  name: Get Category
  type: http
  seq: 10
}

get {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/categories/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
