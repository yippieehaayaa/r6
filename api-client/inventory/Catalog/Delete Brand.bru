meta {
  name: Delete Brand
  type: http
  seq: 16
}

delete {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/brands/{{brandId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
