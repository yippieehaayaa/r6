meta {
  name: Delete Product
  type: http
  seq: 4
}

delete {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/products/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
