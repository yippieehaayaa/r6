meta {
  name: Delete Variant
  type: http
  seq: 8
}

delete {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/variants/{{variantId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
