meta {
  name: Get Product
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/tenants/{{tenantSlug}}/catalog/products/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
