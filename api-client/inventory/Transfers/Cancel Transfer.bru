meta {
  name: Cancel Transfer
  type: http
  seq: 5
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/transfers/{{transferId}}/cancel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {}
}
