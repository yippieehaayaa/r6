meta {
  name: Reserve Stock
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/stock-out/reserve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "referenceId": "ORDER-2024-001",
    "referenceType": "SALE_ORDER",
    "lines": [
      {
        "variantId": "{{variantId}}",
        "warehouseId": "{{warehouseId}}",
        "quantity": 5
      }
    ]
  }
}
