meta {
  name: Acknowledge Alert
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/stock-alerts/{{alertId}}/acknowledge
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "notes": "Reorder placed"
  }
}
