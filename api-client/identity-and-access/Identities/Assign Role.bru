meta {
  name: Assign Role
  type: http
  seq: 7
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/identities/{{identityId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "roleId": "{{roleId}}"
  }
}
