meta {
  name: Create Identity
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/tenants/{{tenantSlug}}/identities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "username": "johndoe",
    "plainPassword": "Password1!",
    "email": "johndoe@exdsaom"
  }
}
