meta {
  name: Restore Identity
  type: http
  seq: 6
}

post {
  url: {{baseURL}}/tenants/{{tenantId}}/identities/{{identityId}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
