meta {
  name: Enable TOTP
  type: http
  seq: 5
}

post {
  url: {{baseURL}}/me/totp/enable
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "code": "123456"
  }
}
