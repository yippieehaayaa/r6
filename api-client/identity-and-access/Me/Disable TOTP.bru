meta {
  name: Disable TOTP
  type: http
  seq: 6
}

delete {
  url: {{baseURL}}/me/totp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "password": "YourPassword1!"
  }
}
