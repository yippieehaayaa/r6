meta {
  name: Create Role
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/tenants/{{tenantId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "editor",
    "description": "Can read and write resources"
  }
}
