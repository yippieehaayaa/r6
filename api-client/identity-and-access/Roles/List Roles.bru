meta {
  name: List Roles
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/tenants/{{tenantId}}/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  page: 1
  limit: 20
}
