meta {
  name: Verify TOTP
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/auth/totp/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "code": "123456"
  }
}
