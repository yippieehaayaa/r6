meta {
  name: Create Tenant
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/tenants
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Acme Corp",
    "slug": "acme-corp",
    "moduleAccess": ["inventory", "procurement"]
  }
}
