meta {
  name: Update Tenant
  type: http
  seq: 4
}

patch {
  url: {{baseURL}}/tenants/{{tenantId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Acme Corporation",
    "slug": "acme-corporation",
    "isActive": true,
    "moduleAccess": ["inventory", "procurement", "catalog"]
  }
}
