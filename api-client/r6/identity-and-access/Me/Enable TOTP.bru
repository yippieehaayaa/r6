meta {
  name: Enable TOTP
  type: http
  seq: 4
}

post {
  url: {{iamURL}}/me/totp/enable
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "code": "123456"
  }
}
