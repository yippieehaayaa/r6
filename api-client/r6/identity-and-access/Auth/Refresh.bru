meta {
  name: Refresh
  type: http
  seq: 3
}

post {
  url: {{iamURL}}/auth/refresh
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("accessToken", res.body.accessToken);
  }
}
